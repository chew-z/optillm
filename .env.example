# Z.ai API Configuration
ZAI_API_KEY=your_zai_api_key_here
ZAI_BASE_URL=https://api.z.ai/api/coding/paas/v4
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OptiLLM Configuration
OPTILLM_PORT=8000
OPTILLM_MODEL=glm-4.7

# Complex Model Configuration
# Each JSON file maps profile names (e.g., "rapid", "deep") to role arrays.
# Override these paths to point at custom files if desired.
OPTILLM_BON_CONFIG_FILE=./optillm/config/bon_profiles.json
OPTILLM_MOA_CONFIG_FILE=./optillm/config/moa_profiles.json

# Usage Examples:
#
# 1. Use predefined complex model from JSON files:
#    model="bon-rapid"   → Uses the "rapid" profile in OPTILLM_BON_CONFIG_FILE
#    model="bon-deep"    → Uses the "deep" profile in OPTILLM_BON_CONFIG_FILE
#    model="moa-coding"  → Uses the "coding" profile in OPTILLM_MOA_CONFIG_FILE
#
# 2. Use specific model for all roles (homogeneous):
#    model="zai/glm-4.7" with approach="bon" → All four candidates plus raters use zai/glm-4.7
#    model="gpt-4o" with approach="moa"      → All agents use gpt-4o
#
# 3. Fallback to default when models/configs are missing:
#    Uses OPTILLM_MODEL (glm-4.7) for all candidates/agents
